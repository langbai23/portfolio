<script>
  // @ts-nocheck — plain browser script, no types.
  /**
   * Two translation mechanisms:
   *  1. Static UI strings → `data-i18n="key"` + the dictionary below.
   *  2. Project content   → `data-i18n-dual` + `data-en` / `data-zh`
   *     attributes rendered from src/data/projects.ts (see T.astro).
   */
  const translations = {
    en: {
      navProjects: "Projects",
      navAbout: "About",
      navContact: "Contact",
      navResume: "Resume",
      heroEyebrow: "Game Designer & Developer · USC",
      heroTagline: "Stories told through systems.",
      heroIntro:
        "USC freshman making games where narrative craft and system design pull in the same direction. Unity, tabletop, and everything between.",
      heroPrimaryBtn: "View Projects",
      heroSecondaryBtn: "Contact Me",
      heroCardTitle: "Currently",
      heroCardItem1: "Prototyping narrative systems in Unity 3D",
      heroCardItem2: "Studying Game Design at USC · first year",
      heroCardItem3: "Exploring where mechanics and story converge",
      projectsEyebrow: "Selected Work",
      projectsTitle: "Projects",
      projectsSubtitle:
        "Latest work first. Each project opens a case study with my role, process, and footage.",
      featuredLabel: "Latest",
      caseStudyCta: "Read case study",
      viewAllProjects: "View all projects",
      projectsAllTitle: "All projects",
      projectsAllSubtitle: "Complete list in chronological order, newest to oldest.",
      projectHoverPreviewHint: "Hover to preview",
      aboutEyebrow: "About",
      aboutTitle: "About Me",
      aboutP1:
        "I'm a freshman at USC studying game design. I'm most passionate about storytelling and system design—emotion and meaning, expressed through rules, loops, and feedback players can actually feel.",
      aboutP2:
        "I care most when mechanics and narrative pull the same direction: clear systems, and story beats that land because the design earns them—not just the script.",
      aboutP3:
        "Lately that's Unity: small prototypes where I test narrative and systems in the same build and iterate fast.",
      aboutP4:
        "I'm always learning from games that marry story with systems—and I'd love to meet others who care about that mix too.",
      aboutStatGamesLabel: "Projects Built",
      aboutStatJamValue: "1",
      aboutStatJamLabel: "Game Jam",
      aboutStatYearsValue: "3+",
      aboutStatYearsLabel: "Years Learning",
      aboutStatIdeasValue: "±∞",
      aboutStatIdeasLabel: "Ideas in Progress",
      skillsTitle: "Tools & Skills",
      aboutPhotoAlt: "David Bao at Ubisoft Shanghai",
      contactEyebrow: "Contact",
      contactTitle: "Let's Connect",
      contactSubtitle:
        "Open to game design and development internships. Reach me by email, or connect on LinkedIn.",
      contactLinkedInLabel: "LinkedIn",
      contactGithubLabel: "GitHub",
      contactResumeLabel: "Resume (PDF)",
      csBack: "← Back to projects",
      csOverview: "Overview",
      csRoleLabel: "Role",
      csEngineLabel: "Engine",
      csYearLabel: "Year",
      csWatchOnYoutube: "Watch full video on YouTube ↗",
      csInProgressTitle: "Full case study in progress",
      csInProgressBody:
        "A deeper write-up of this project—design decisions, process, and screenshots—is on its way.",
      footerText: "Built by David Bao with Astro",
    },
    zh: {
      navProjects: "项目",
      navAbout: "关于",
      navContact: "联系",
      navResume: "简历",
      heroEyebrow: "游戏设计师与开发者 · USC",
      heroTagline: "用系统讲故事。",
      heroIntro:
        "USC 大一，做游戏——让叙事与系统设计朝同一个方向发力。Unity、桌游，以及两者之间的一切。",
      heroPrimaryBtn: "查看项目",
      heroSecondaryBtn: "联系我",
      heroCardTitle: "最近在做",
      heroCardItem1: "在 Unity 3D 里打造叙事系统原型",
      heroCardItem2: "就读于 USC 游戏设计专业 · 大一",
      heroCardItem3: "寻找机制与故事的交汇点",
      projectsEyebrow: "精选作品",
      projectsTitle: "项目",
      projectsSubtitle: "最新作品优先。每个项目都有 case study：我的角色、过程与实机画面。",
      featuredLabel: "最新",
      caseStudyCta: "阅读 case study",
      viewAllProjects: "查看全部项目",
      projectsAllTitle: "全部项目",
      projectsAllSubtitle: "按时间顺序排列，从新到旧。",
      projectHoverPreviewHint: "悬停预览",
      aboutEyebrow: "关于",
      aboutTitle: "关于我",
      aboutP1:
        "我是 USC 的大一游戏设计学生。最投入的是叙事与系统设计——用情感、意义，以及玩家能感知到的规则、循环与反馈来表达。",
      aboutP2: "最喜欢机制与叙事同向发力：系统清晰，剧情节点靠设计托住，而不只靠台词。",
      aboutP3: "最近在 Unity 做小原型，在同一套可玩循环里试叙事与系统，快速迭代。",
      aboutP4: "一直在向优秀作品学习如何把故事与系统拧在一起；也欢迎同样在意这条线的人交流。",
      aboutStatGamesLabel: "完成项目",
      aboutStatJamValue: "1",
      aboutStatJamLabel: "游戏 Jam",
      aboutStatYearsValue: "3+",
      aboutStatYearsLabel: "学习年限",
      aboutStatIdeasValue: "±∞",
      aboutStatIdeasLabel: "进行中想法",
      skillsTitle: "工具与技能",
      aboutPhotoAlt: "David Bao 在育碧上海",
      contactEyebrow: "联系",
      contactTitle: "欢迎联系",
      contactSubtitle: "正在寻找游戏设计与开发方向的实习机会。可以通过邮箱或 LinkedIn 联系我。",
      contactLinkedInLabel: "LinkedIn",
      contactGithubLabel: "GitHub",
      contactResumeLabel: "简历 (PDF)",
      csBack: "← 返回项目",
      csOverview: "概览",
      csRoleLabel: "角色",
      csEngineLabel: "引擎",
      csYearLabel: "年份",
      csWatchOnYoutube: "在 YouTube 观看完整视频 ↗",
      csInProgressTitle: "完整 case study 制作中",
      csInProgressBody: "这个项目更深入的复盘——设计决策、过程与截图——正在路上。",
      footerText: "David Bao 使用 Astro 构建",
    },
  };

  const LANG_KEY = "site-lang";
  const langButtons = document.querySelectorAll("[data-lang]");

  const setLanguage = (lang) => {
    const dict = translations[lang];
    if (!dict) return;

    document.querySelectorAll("[data-i18n]").forEach((node) => {
      const key = node.getAttribute("data-i18n");
      if (key && dict[key]) node.textContent = dict[key];
    });

    document.querySelectorAll("[data-i18n-alt]").forEach((node) => {
      const key = node.getAttribute("data-i18n-alt");
      if (key && dict[key]) node.setAttribute("alt", dict[key]);
    });

    document.querySelectorAll("[data-i18n-dual]").forEach((node) => {
      const text = node.getAttribute(lang === "zh" ? "data-zh" : "data-en");
      if (text) node.textContent = text;
    });

    document.documentElement.setAttribute("lang", lang === "zh" ? "zh-CN" : "en");

    langButtons.forEach((button) => {
      button.classList.toggle("active", button.getAttribute("data-lang") === lang);
    });

    try {
      localStorage.setItem(LANG_KEY, lang);
    } catch {
      /* private mode etc. — ignore */
    }
  };

  langButtons.forEach((button) => {
    button.addEventListener("click", () => {
      const lang = button.getAttribute("data-lang");
      if (lang) setLanguage(lang);
    });
  });

  let savedLang = "en";
  try {
    savedLang = localStorage.getItem(LANG_KEY) === "zh" ? "zh" : "en";
  } catch {
    /* ignore */
  }
  setLanguage(savedLang);

  // Home links use /#projects, /#about, etc. The hash stays in the URL, so refresh jumps to that section.
  // On full reload only, strip the hash and scroll to the top so refresh returns to the hero.
  window.addEventListener("pageshow", () => {
    const nav = performance.getEntriesByType("navigation")[0];
    const isReload =
      (nav && nav.type === "reload") ||
      (typeof performance.navigation !== "undefined" && performance.navigation.type === 1);
    if (!isReload || !window.location.hash) return;
    const path = window.location.pathname;
    if (path !== "/" && !path.endsWith("/index.html")) return;
    history.replaceState(null, "", `${path}${window.location.search}`);
    window.scrollTo(0, 0);
  });

  const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const revealItems = document.querySelectorAll(".reveal-item");

  if (prefersReducedMotion) {
    revealItems.forEach((item) => item.classList.add("is-visible"));
  } else {
    const revealObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("is-visible");
            revealObserver.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.2, rootMargin: "0px 0px -20px 0px" }
    );

    revealItems.forEach((item) => revealObserver.observe(item));
  }

  // Cover images: fall back to data-fallback-src if the primary fails
  // (YouTube maxresdefault.jpg isn't available for every video).
  document.querySelectorAll("img[data-fallback-src]").forEach((img) => {
    img.addEventListener(
      "error",
      () => {
        const fallback = img.getAttribute("data-fallback-src");
        if (fallback && img.getAttribute("src") !== fallback) {
          img.setAttribute("src", fallback);
        }
      },
      { once: true }
    );
    // If it already failed before this script ran:
    if (img.complete && img.naturalWidth === 0) {
      const fallback = img.getAttribute("data-fallback-src");
      if (fallback && img.getAttribute("src") !== fallback) {
        img.setAttribute("src", fallback);
      }
    }
  });

  const canHoverPreview = window.matchMedia("(hover: hover) and (pointer: fine)").matches;
  const previewMediaNodes = document.querySelectorAll("[data-preview-video-id]");

  previewMediaNodes.forEach((mediaNode) => {
    const slot = mediaNode.querySelector("[data-preview-slot]");
    const videoId = mediaNode.getAttribute("data-preview-video-id");
    const videoTitle = mediaNode.getAttribute("data-preview-title") || "Project preview";
    const previewMode = mediaNode.getAttribute("data-preview-mode") || "autoplay";
    const previewStart = mediaNode.getAttribute("data-preview-start");
    const previewEnd = mediaNode.getAttribute("data-preview-end");
    const previewSegments = mediaNode.getAttribute("data-preview-segments");
    let iframe = null;
    let segmentTimerId = null;
    let repeatTimerId = null;
    let currentSegmentIndex = 0;

    if (!slot || !videoId) return;

    const segments = previewSegments
      ? previewSegments
          .split(",")
          .map((rawSegment) => rawSegment.trim())
          .map((segment) => {
            const [startRaw, endRaw] = segment.split("-");
            const start = Number(startRaw);
            const end = Number(endRaw);
            return Number.isFinite(start) && Number.isFinite(end) && end > start
              ? { start, end }
              : null;
          })
          .filter(Boolean)
      : [];

    const buildIframeSrc = (startValue, endValue) => {
      const params = new URLSearchParams({
        autoplay: "1",
        mute: "1",
        controls: "0",
        modestbranding: "1",
        rel: "0",
        playsinline: "1"
      });

      const hasClipBounds =
        typeof startValue === "number" &&
        typeof endValue === "number" &&
        Number.isFinite(startValue) &&
        Number.isFinite(endValue);

      if (typeof startValue === "number" && Number.isFinite(startValue)) {
        params.set("start", String(Math.floor(startValue)));
      }
      if (typeof endValue === "number" && Number.isFinite(endValue)) {
        params.set("end", String(Math.ceil(endValue)));
      }

      if (!hasClipBounds) {
        params.set("loop", "1");
        params.set("playlist", videoId);
      }

      return `https://www.youtube.com/embed/${videoId}?${params.toString()}`;
    };

    const renderIframe = (startValue, endValue) => {
      if (iframe) {
        iframe.remove();
        iframe = null;
      }

      iframe = document.createElement("iframe");
      iframe.src = buildIframeSrc(startValue, endValue);
      iframe.title = videoTitle;
      iframe.loading = "lazy";
      iframe.allow = "autoplay; encrypted-media; picture-in-picture";
      iframe.referrerPolicy = "strict-origin-when-cross-origin";
      slot.appendChild(iframe);
      mediaNode.classList.add("is-playing");
    };

    const clearTimers = () => {
      if (segmentTimerId) {
        window.clearTimeout(segmentTimerId);
        segmentTimerId = null;
      }
      if (repeatTimerId) {
        window.clearTimeout(repeatTimerId);
        repeatTimerId = null;
      }
    };

    const playSingleClip = () => {
      const singleStart = previewStart ? Number(previewStart) : NaN;
      const singleEnd = previewEnd ? Number(previewEnd) : NaN;

      if (Number.isFinite(singleStart) && Number.isFinite(singleEnd)) {
        renderIframe(singleStart, singleEnd);
        repeatTimerId = window.setTimeout(() => {
          repeatTimerId = null;
          playSingleClip();
        }, Math.max(800, (singleEnd - singleStart) * 1000));
        return;
      }

      renderIframe(
        Number.isFinite(singleStart) ? singleStart : undefined,
        Number.isFinite(singleEnd) ? singleEnd : undefined
      );
    };

    const playCurrentSegment = () => {
      if (!segments.length) {
        playSingleClip();
        return;
      }

      const currentSegment = segments[currentSegmentIndex];
      renderIframe(currentSegment.start, currentSegment.end);

      const segmentDurationMs = Math.max(800, (currentSegment.end - currentSegment.start) * 1000);
      segmentTimerId = window.setTimeout(() => {
        segmentTimerId = null;
        currentSegmentIndex = (currentSegmentIndex + 1) % segments.length;
        playCurrentSegment();
      }, segmentDurationMs);
    };

    const startPreview = () => {
      if (iframe || segmentTimerId || repeatTimerId) return;
      currentSegmentIndex = 0;
      playCurrentSegment();
    };

    const stopPreview = () => {
      clearTimers();
      if (iframe) {
        iframe.remove();
        iframe = null;
      }
      mediaNode.classList.remove("is-playing");
    };

    if (previewMode === "hover") {
      if (!canHoverPreview) return;
      mediaNode.addEventListener("mouseenter", startPreview);
      mediaNode.addEventListener("mouseleave", stopPreview);
      mediaNode.addEventListener("focusin", startPreview);
      mediaNode.addEventListener("focusout", stopPreview);
      return;
    }

    const previewObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            startPreview();
          } else {
            stopPreview();
          }
        });
      },
      { threshold: 0.35, rootMargin: "0px 0px -10% 0px" }
    );

    previewObserver.observe(mediaNode);
  });
</script>
