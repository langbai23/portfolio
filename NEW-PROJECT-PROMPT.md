# 新项目上线：问题模板

做完一个游戏后，把 **「复制给游戏 agent」以下的全部内容** 粘贴给当时做那个游戏的 agent，
再把它的回答整段复制回作品集这边。拿到回答后我负责写中英双语文案、填进
`src/data/projects.ts`、更新 itch 页面。

素材（视频/截图）不用你手动整理规格 —— 提示词里已经让它检查并建好文件夹。

---

## 复制给游戏 agent（从这里开始）

我要把这个游戏放进我的作品集网站和 itch.io 页面。请你基于这个仓库的实际代码和内容
回答下面的问题。你的回答会被整段复制走，所以请按标题分段、用纯文本，不要用表格。

### 一、这个游戏是什么

1. 一句话说清前提：玩家是谁，在做什么，赌注是什么。
2. 核心循环：玩家在一局里反复做的事，具体到操作层面。
3. 最重要的那个设计决定是什么？为什么这么定，而不是另一种做法？
4. 做的过程中最大的难题是什么，最后怎么解决的？如果推翻重做过，说明推翻了什么。
5. 你守了哪些约束（技术的、美术的、时间的、课程要求的）？哪条约束反而帮到了设计？

回答 3 和 5 时请具体。「做了一个有趣的战斗系统」没有信息量；
「敌人行为完全确定、没有随机，所以打得难看永远不能怪运气」才有。

### 二、事实信息

- 引擎和版本（例：Unity 6000.6.0f1）
- 独立完成还是团队？团队的话几个人、你具体负责哪部分
- 时间跨度、是否课程作业（哪门课）
- 支持平台
- **操作方式**：精确到按键和鼠标操作，一步步写清楚
- 安装注意事项（例：macOS 未公证需要右键打开；Windows 需解压后运行某个 exe）
- 游戏目前的状态：已完成 / 还在做 / 原型 / 搁置
- 一局大概多长时间（只在你确实知道时回答）

### 三、不要编造任何数字

**这条最重要。** 如果某个数据你没有真的跑过或测过，就直接说「没测过」。

不要写「经过 N 次 playtest」「平均通关时间 X 分钟」「回归测试显示剩 Y 点血」
这类听起来很具体、实际是推测的内容。这些文案会公开给招聘方看，
被追问一句就崩了。**宁可没有数字，也不要有假数字。**

设计意图可以写（「调参目标是让两条路线都险胜」），
测量结果不要编（「实测两条路线都剩 7 点血」）。

### 四、素材检查

检查这个仓库里有没有以下东西，并如实告诉我哪些有、哪些没有：

- 游戏演示视频（任何格式）
- 截图
- GIF
- 封面图

**如果缺任何一项**，请在仓库根目录建一个 `portfolio-media/` 文件夹，
在里面放一个 `README.md` 写清楚还缺什么、需要什么规格，然后告诉我文件夹路径。
我会自己把文件放进去。

规格要求：

- `clip.mp4` — 1280×720，**无音轨**，H.264，10 到 15 秒，展示核心玩法的片段
- `cover.jpg` — 1280×720，一张最能代表这个游戏的画面
- `01.png` 到 `05.png` — 3 到 5 张截图，按游戏流程顺序排（开场 → 核心玩法 → 关键时刻 → 结局）

如果你**有能力自己录制或导出**这些素材（比如工程里有录屏、或你能跑构建截图），
就直接按规格生成放进 `portfolio-media/`，不用等我。

### 五、itch.io 页面补充

- 推荐的 Genre（从这些里选一个）：Action / Adventure / Card Game / Educational /
  Fighting / Interactive Fiction / Platformer / Puzzle / Racing / Rhythm /
  Role Playing / Shooter / Simulation / Sports / Strategy / Survival /
  Visual Novel / Other
- 推荐 5 个左右的标签（小写、用连字符，例：`turn-based-combat`、`low-poly`）
- 如果游戏已经发布在 itch 上，给我页面地址

（复制到这里结束）

---

## 拿到回答之后

把 agent 的回答整段发给我就行。我会：

1. 写成中英双语，按 `projects.ts` 的 schema 填进去（参照 Glimmer 那条的结构）
2. 处理素材：你给我文件，或告诉我 `portfolio-media/` 的路径
3. 更新 itch 页面：描述、Genre、Tags、发布状态
4. 本地构建验证，等你确认后再 push

如果 agent 在某些问题上回答「没测过」「不知道」，那是对的，不用回去追问。
文案宁可少一句，也不要有编的内容。
